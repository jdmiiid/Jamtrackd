import 'package:flutter/material.dart';

class FuffPage extends StatelessWidget {
  const FuffPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('data'));
  }
}
